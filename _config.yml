title: Eclipse Guide
# Remove the standard theme line to avoid conflicts
remote_theme: just-the-docs/just-the-docs

plugins:
  - jekyll-remote-theme

color_scheme: dark
nav_enabled: true  # Explicitly enable the sidebar
